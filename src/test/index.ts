// Barrel export for all mock hooks
// Sử dụng: import { useMockEmployees, useMockDashboard } from "@/test";

export { useMockEmployees } from "@/hooks/useMockEmployees";
export { useMockAttendance } from "@/hooks/useMockAttendance";
export { useMockDashboard } from "@/hooks/useMockDashboard";
export { useMockDepartments } from "@/hooks/useMockDepartments";
export { useMockPayroll } from "@/hooks/useMockPayroll";
export { useMockRecruitment } from "@/hooks/useMockRecruitment";
export { useMockPerformance } from "@/hooks/useMockPerformance";
export { useMockRoles } from "@/hooks/useMockRoles";

// Mock data exports
export * from "./mockData";
export * from "./mockServices";
